apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerTemplate
metadata:
  name: il-gitlab-echo-template
spec:
  params:
    - name: gitrevision
    - name: gitrepositoryurl
  resourcetemplates:
    - apiVersion: tekton.dev/v1alpha1
      kind: TaskRun
      metadata:
        generateName: il-gitlab-run-
      spec:
        taskSpec:
          inputs:
            resources:
              - name: source
                type: git
          steps:
            - image: ubuntu
              script: |
                #! /bin/bash
                ls -al $(inputs.resources.source.path)
        inputs:
          resources:
            - name: source
              resourceSpec:
                type: git
                params:
                  - name: revision
                    value: $(params.gitrevision)
                  - name: url
                    value: $(params.gitrepositoryurl)
---
apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerBinding
metadata:
  name: il-gitlab-push-binding
spec:
  params:
    - name: gitrevision
      value: $(body.checkout_sha)
    - name: gitrepositoryurl
      value: $(body.repository.git_http_url)
---
apiVersion: v1
kind: Secret
metadata:
  name: il-gitlab-secret
type: Opaque
stringData:
  gitlabToken: 123intel
---
apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: il-gitlab-listener
spec:
  # from examples/role-resources/servicaccount.yaml
  serviceAccountName: il-tekton-ci-sa
  triggers:
    - name: il-gitlab-push-events-trigger
      interceptors:
        - gitlab:
            secretRef:
              secretName: il-gitlab-secret
              secretKey: gitlabToken
            eventTypes:
              - Push Hook  # Only push events
      bindings:
        - name: il-gitlab-push-binding
      template:
        name: il-gitlab-echo-template
  serviceType: NodePort
